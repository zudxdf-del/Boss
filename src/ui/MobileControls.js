export class MobileControls { constructor(input){this.input=input;document.querySelectorAll('[data-key]').forEach(b=>{const k=b.dataset.key;b.addEventListener('pointerdown',e=>{e.preventDefault();input.keys.add(k==='up'?'w':k==='down'?'s':k==='left'?'a':'d');});b.addEventListener('pointerup',e=>{e.preventDefault();input.keys.delete(k==='up'?'w':k==='down'?'s':k==='left'?'a':'d');});b.addEventListener('pointercancel',()=>input.keys.clear());});const fire=document.querySelector('#fire-button');fire.addEventListener('pointerdown',e=>{e.preventDefault();input.pointer.down=true;});fire.addEventListener('pointerup',()=>input.pointer.down=false);fire.addEventListener('pointercancel',()=>input.pointer.down=false);} }
